## CONCERNS
- Cost estimation in `reviewer.py` uses a flat rate ($1/$3 per 1M input/output tokens) for all models. Real rates vary significantly per model. This is acceptable per spec ("rough estimate") but will be inaccurate for cheap models like DeepSeek.
- The `echo` in the test wrote literal `\n` rather than newlines (shell behavior), but `--dry-run` still worked correctly since it just reads whatever is in the files.

## FILES MODIFIED
- `pyproject.toml` (modified — added httpx>=0.27.0 dependency)
- `src/playbook/reviewer.py` (created — API client, model panel, prompt assembly, response parsing)
- `src/playbook/commands/review.py` (created — Click command with --model and --dry-run flags)
- `src/playbook/cli.py` (modified — imported and registered review-spec command)

## VERIFICATION
- `pip install -e ".[dev]"` — succeeded, httpx 0.28.1 installed
- `playbook --help` — shows `review-spec` in command list
- `playbook review-spec --help` — shows --model and --dry-run flags
- `playbook review-spec --dry-run` — prints prompt, model list, and token estimates (no API calls)
- Not-in-project error → exit code 1
- Missing SPEC.md/TASKS.md → exit code 3
- `pytest tests/ -q` — 38 passed, no regressions

## STATUS
COMPLETE
