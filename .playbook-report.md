# Task 009: Review-Spec Tests + Live Verification

## CONCERNS

- The task spec references mocking `httpx.Client.post`, but the actual code uses `httpx.post` (module-level function, not a Client instance). Tests mock the correct target (`playbook.reviewer.httpx.post`).
- The dry-run output says "Dry Run" (title case), not "DRY RUN" (all caps) as the task description implies. Tests match the actual code behavior.

## FILES MODIFIED

- tests/test_review.py (created) — 7 tests covering all deliverables

## VERIFICATION

- `pytest tests/test_review.py -v` — 7/7 passed
- `pytest -v` — 45/45 passed (no regressions)
- Live test with `deepseek/deepseek-chat` on a temp project:
  - `reviews/spec-review-deepseek.md` created successfully
  - File contains model's critical review (72 issues found on template spec)
  - Terminal showed issue count and cost estimate (~$0.008)

## STATUS

COMPLETE
