# Playbook CLI — done + verify Commands Report

## Changes Made

### Command 1: playbook task done <NNN>
- **File:** src/playbook/commands/task.py — added done subcommand to task_group
- **File:** src/playbook/parser.py — added update_task_status() function
- Accepts task number (e.g., 3 or 003), zero-pads to 3 digits
- Updates the matching row status to ✓ in TASKS.md
- Exit 1 if not in project, exit 2 if task not found

### Command 2: playbook verify <NNN>
- **File:** src/playbook/commands/verify.py — new file
- **File:** src/playbook/cli.py — registered verify_cmd
- Reads task file matching NNN from tasks/ directory
- Extracts bash code block under ## Verify section
- Executes each line via subprocess.run(shell=True, cwd=project_root)
- Reports PASS/FAIL per command; overall exit 0 if all pass, exit 1 if any fail
- Exits 0 with message if no Verify section found

### Tests Added
- tests/test_task.py — 3 new tests for done command (12 total)
- tests/test_verify.py — 3 new tests for verify command

## Test Results

51 passed in 1.52s

All existing tests continue to pass. No regressions.
