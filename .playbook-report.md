## CONCERNS
- None

## FILES MODIFIED
- src/playbook/commands/relay.py (modified — added --send flag)
- src/playbook/commands/check.py (created — new check command)
- src/playbook/cli.py (modified — registered check command)
- tests/test_relay.py (modified — added 3 tests for --send)
- tests/test_check.py (created — 3 tests for check command)
- .gitignore (modified — added .relay-prompt.txt and .playbook-report.md)

## VERIFICATION
- Full test suite: 57/57 passed (including 6 new tests)
- relay --send: creates .relay-prompt.txt, prints summary, does NOT print prompt to stdout
- relay --send --copy: correctly errors as mutually exclusive
- relay --send: updates .gitignore if .relay-prompt.txt not listed
- check: sends task + report to Gemini 3 Flash via OpenRouter, prints Rich output, writes review file
- check with no report: exits non-zero with "No agent report found"
- check outside project: exits non-zero

## STATUS
COMPLETE
